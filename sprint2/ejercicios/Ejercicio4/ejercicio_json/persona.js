const slugify = require("slugify");

const jsObject = {
  firstname: "Maria",
  lastname: "López",
  age: 45,
};

// const objectWithSlugify = slugify(jsObject, {});

module.exports = {
  // objectWithSlugify,
  jsObject,
};
